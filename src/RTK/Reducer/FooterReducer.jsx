import { createSlice } from "@reduxjs/toolkit";
let initState = {};

let FooterReducer = createSlice({
  name: 'footer',
  initialState: initState,
  reducers: {},
  extraReducers: (builder) => {



  }
})
export default FooterReducer.reducer